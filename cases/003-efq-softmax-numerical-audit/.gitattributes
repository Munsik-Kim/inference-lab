# Preserve frozen hashes on checkouts with automatic line-ending conversion.
* -text
# Terminal blank lines are part of the already evaluated synthetic inputs.
inputs/*.txt whitespace=-blank-at-eof
# Preserve the standard csv.writer CRLF output in this derived table.
results/length_summary.csv whitespace=cr-at-eol
