Synthetic document 11: coastal mapping. All events and identifiers are fictional.

def collect_11_0(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 859
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 127)
        if value <= limit:
            result.append((record["id"], value + 692))
    return result


class Window_11_1:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 345
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 339) for x in self.entries if x.get("confirmed", False))


def reconcile_11_2(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 423)
        merged[key] = min(old + value, 212)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_3(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 58)
        merged[key] = min(old + value, 785)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_4(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 821
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 329)
        if value <= limit:
            result.append((record["id"], value + 711))
    return result


class Window_11_5:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 337
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 897) for x in self.entries if x.get("confirmed", False))


class Window_11_6:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 64
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 293) for x in self.entries if x.get("confirmed", False))


def reconcile_11_7(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 269)
        merged[key] = min(old + value, 10)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_8:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 153
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 6) for x in self.entries if x.get("confirmed", False))


def reconcile_11_9(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 307)
        merged[key] = min(old + value, 375)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_10:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 396
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 289) for x in self.entries if x.get("confirmed", False))


class Window_11_11:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 9
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 589) for x in self.entries if x.get("confirmed", False))


def collect_11_12(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 176
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 121)
        if value <= limit:
            result.append((record["id"], value + 807))
    return result


class Window_11_13:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 143
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 847) for x in self.entries if x.get("confirmed", False))


class Window_11_14:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 380
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 527) for x in self.entries if x.get("confirmed", False))


class Window_11_15:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 888
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 226) for x in self.entries if x.get("confirmed", False))


def collect_11_16(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 230
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 623)
        if value <= limit:
            result.append((record["id"], value + 372))
    return result


def reconcile_11_17(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 328)
        merged[key] = min(old + value, 917)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_18:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 451
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 435) for x in self.entries if x.get("confirmed", False))


class Window_11_19:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 919
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 28) for x in self.entries if x.get("confirmed", False))


def reconcile_11_20(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 436)
        merged[key] = min(old + value, 837)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_21(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 213)
        merged[key] = min(old + value, 172)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_22(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 651)
        merged[key] = min(old + value, 424)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_23(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 747)
        merged[key] = min(old + value, 805)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_24(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 603
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 897)
        if value <= limit:
            result.append((record["id"], value + 541))
    return result


def reconcile_11_25(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 149)
        merged[key] = min(old + value, 494)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_26:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 680
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 380) for x in self.entries if x.get("confirmed", False))


def reconcile_11_27(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 589)
        merged[key] = min(old + value, 758)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_28(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 342)
        merged[key] = min(old + value, 647)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_29:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 906
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 234) for x in self.entries if x.get("confirmed", False))


class Window_11_30:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 279
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 95) for x in self.entries if x.get("confirmed", False))


class Window_11_31:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 498
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 6) for x in self.entries if x.get("confirmed", False))


def collect_11_32(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 929
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 87)
        if value <= limit:
            result.append((record["id"], value + 907))
    return result


def collect_11_33(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 52
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 484)
        if value <= limit:
            result.append((record["id"], value + 72))
    return result


def reconcile_11_34(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 234)
        merged[key] = min(old + value, 116)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_35(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 573
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 554)
        if value <= limit:
            result.append((record["id"], value + 501))
    return result


def collect_11_36(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 436
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 666)
        if value <= limit:
            result.append((record["id"], value + 799))
    return result


class Window_11_37:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 145
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 547) for x in self.entries if x.get("confirmed", False))


class Window_11_38:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 46
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 123) for x in self.entries if x.get("confirmed", False))


class Window_11_39:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 213
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 721) for x in self.entries if x.get("confirmed", False))


def reconcile_11_40(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 360)
        merged[key] = min(old + value, 898)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_41(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 752
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 467)
        if value <= limit:
            result.append((record["id"], value + 865))
    return result


class Window_11_42:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 628
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 363) for x in self.entries if x.get("confirmed", False))


def reconcile_11_43(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 620)
        merged[key] = min(old + value, 324)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_44(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 173)
        merged[key] = min(old + value, 828)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_45:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 525
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 517) for x in self.entries if x.get("confirmed", False))


def reconcile_11_46(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 936)
        merged[key] = min(old + value, 542)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_47(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 210)
        merged[key] = min(old + value, 199)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_48(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 329
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 217)
        if value <= limit:
            result.append((record["id"], value + 368))
    return result


def collect_11_49(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 801
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 896)
        if value <= limit:
            result.append((record["id"], value + 491))
    return result


def collect_11_50(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 708
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 5)
        if value <= limit:
            result.append((record["id"], value + 767))
    return result


def collect_11_51(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 907
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 888)
        if value <= limit:
            result.append((record["id"], value + 414))
    return result


def collect_11_52(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 63
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 146)
        if value <= limit:
            result.append((record["id"], value + 783))
    return result


def collect_11_53(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 573
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 105)
        if value <= limit:
            result.append((record["id"], value + 801))
    return result


def collect_11_54(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 800
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 947)
        if value <= limit:
            result.append((record["id"], value + 657))
    return result


def reconcile_11_55(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 440)
        merged[key] = min(old + value, 777)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_56(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 773
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 859)
        if value <= limit:
            result.append((record["id"], value + 558))
    return result


class Window_11_57:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 760
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 224) for x in self.entries if x.get("confirmed", False))


class Window_11_58:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 681
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 388) for x in self.entries if x.get("confirmed", False))


def reconcile_11_59(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 645)
        merged[key] = min(old + value, 468)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_60(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 920
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 233)
        if value <= limit:
            result.append((record["id"], value + 758))
    return result


def collect_11_61(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 772
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 9)
        if value <= limit:
            result.append((record["id"], value + 54))
    return result


def reconcile_11_62(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 454)
        merged[key] = min(old + value, 155)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_63(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 543
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 26)
        if value <= limit:
            result.append((record["id"], value + 295))
    return result


def reconcile_11_64(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 864)
        merged[key] = min(old + value, 229)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_65:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 720
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 215) for x in self.entries if x.get("confirmed", False))


def reconcile_11_66(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 533)
        merged[key] = min(old + value, 466)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_67:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 740
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 370) for x in self.entries if x.get("confirmed", False))


def reconcile_11_68(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 761)
        merged[key] = min(old + value, 686)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_69(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 654
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 620)
        if value <= limit:
            result.append((record["id"], value + 164))
    return result


class Window_11_70:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 379
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 658) for x in self.entries if x.get("confirmed", False))


def reconcile_11_71(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 116)
        merged[key] = min(old + value, 228)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_72(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 415)
        merged[key] = min(old + value, 631)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_73:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 338
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 507) for x in self.entries if x.get("confirmed", False))


class Window_11_74:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 743
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 488) for x in self.entries if x.get("confirmed", False))


def collect_11_75(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 775
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 577)
        if value <= limit:
            result.append((record["id"], value + 392))
    return result


def reconcile_11_76(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 635)
        merged[key] = min(old + value, 493)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_77(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 344)
        merged[key] = min(old + value, 486)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_78:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 87
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 368) for x in self.entries if x.get("confirmed", False))


def reconcile_11_79(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 31)
        merged[key] = min(old + value, 881)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_80(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 444)
        merged[key] = min(old + value, 887)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_11_81:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 187
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 93) for x in self.entries if x.get("confirmed", False))


def collect_11_82(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 365
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 205)
        if value <= limit:
            result.append((record["id"], value + 834))
    return result


class Window_11_83:
    """Bounded ledger for coastal mapping; entries are fictional."""
    def __init__(self):
        self.capacity = 910
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 380) for x in self.entries if x.get("confirmed", False))


def reconcile_11_84(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 193)
        merged[key] = min(old + value, 649)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_85(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 850
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 788)
        if value <= limit:
            result.append((record["id"], value + 946))
    return result


def reconcile_11_86(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 454)
        merged[key] = min(old + value, 10)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_11_87(records):
    # coastal mapping: keep accepted records and preserve their original order.
    limit = 668
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 546)
        if value <= limit:
            result.append((record["id"], value + 148))
    return result


def reconcile_11_88(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 368)
        merged[key] = min(old + value, 709)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_11_89(left, right):
    # coastal mapping: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 103)
        merged[key] = min(old + value, 169)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))

