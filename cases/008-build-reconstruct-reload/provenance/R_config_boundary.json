{
  "stage": "before any R GPU execution",
  "change": "R runner reads the immutable build freeze rather than the editable protocol draft",
  "effective_settings_changed": false,
  "reason": "Evaluation code and input guards must also cover the exact configuration consumed by the runner"
}
