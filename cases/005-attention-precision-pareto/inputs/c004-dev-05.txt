Synthetic document 05: graph traversal. All events and identifiers are fictional.

def reconcile_5_0(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 491)
        merged[key] = min(old + value, 604)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_1:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 123
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 613) for x in self.entries if x.get("confirmed", False))


class Window_5_2:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 68
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 885) for x in self.entries if x.get("confirmed", False))


def collect_5_3(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 288
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 540)
        if value <= limit:
            result.append((record["id"], value + 400))
    return result


def reconcile_5_4(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 617)
        merged[key] = min(old + value, 722)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_5(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 492
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 225)
        if value <= limit:
            result.append((record["id"], value + 359))
    return result


class Window_5_6:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 764
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 211) for x in self.entries if x.get("confirmed", False))


class Window_5_7:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 546
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 414) for x in self.entries if x.get("confirmed", False))


def collect_5_8(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 780
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 17)
        if value <= limit:
            result.append((record["id"], value + 590))
    return result


def reconcile_5_9(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 201)
        merged[key] = min(old + value, 274)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_10(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 937
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 113)
        if value <= limit:
            result.append((record["id"], value + 241))
    return result


class Window_5_11:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 55
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 529) for x in self.entries if x.get("confirmed", False))


class Window_5_12:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 703
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 45) for x in self.entries if x.get("confirmed", False))


def reconcile_5_13(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 232)
        merged[key] = min(old + value, 256)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_14(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 346
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 486)
        if value <= limit:
            result.append((record["id"], value + 607))
    return result


class Window_5_15:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 643
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 385) for x in self.entries if x.get("confirmed", False))


class Window_5_16:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 59
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 415) for x in self.entries if x.get("confirmed", False))


class Window_5_17:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 559
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 524) for x in self.entries if x.get("confirmed", False))


class Window_5_18:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 144
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 817) for x in self.entries if x.get("confirmed", False))


def collect_5_19(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 839
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 927)
        if value <= limit:
            result.append((record["id"], value + 940))
    return result


class Window_5_20:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 438
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 60) for x in self.entries if x.get("confirmed", False))


def reconcile_5_21(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 413)
        merged[key] = min(old + value, 853)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_22(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 64
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 825)
        if value <= limit:
            result.append((record["id"], value + 892))
    return result


class Window_5_23:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 941
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 654) for x in self.entries if x.get("confirmed", False))


def reconcile_5_24(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 279)
        merged[key] = min(old + value, 98)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_25(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 258
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 505)
        if value <= limit:
            result.append((record["id"], value + 258))
    return result


def reconcile_5_26(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 693)
        merged[key] = min(old + value, 568)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_27:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 300
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 763) for x in self.entries if x.get("confirmed", False))


def collect_5_28(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 617
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 712)
        if value <= limit:
            result.append((record["id"], value + 638))
    return result


class Window_5_29:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 371
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 709) for x in self.entries if x.get("confirmed", False))


def collect_5_30(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 511
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 756)
        if value <= limit:
            result.append((record["id"], value + 921))
    return result


def collect_5_31(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 222
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 772)
        if value <= limit:
            result.append((record["id"], value + 507))
    return result


def reconcile_5_32(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 531)
        merged[key] = min(old + value, 675)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_33(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 802)
        merged[key] = min(old + value, 922)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_34(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 610
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 723)
        if value <= limit:
            result.append((record["id"], value + 665))
    return result


def collect_5_35(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 183
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 751)
        if value <= limit:
            result.append((record["id"], value + 8))
    return result


def collect_5_36(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 249
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 801)
        if value <= limit:
            result.append((record["id"], value + 116))
    return result


class Window_5_37:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 700
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 494) for x in self.entries if x.get("confirmed", False))


def collect_5_38(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 227
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 917)
        if value <= limit:
            result.append((record["id"], value + 684))
    return result


def reconcile_5_39(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 369)
        merged[key] = min(old + value, 725)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_40(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 634)
        merged[key] = min(old + value, 827)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_41(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 943
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 26)
        if value <= limit:
            result.append((record["id"], value + 470))
    return result


def reconcile_5_42(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 718)
        merged[key] = min(old + value, 444)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_43(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 137)
        merged[key] = min(old + value, 765)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_44:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 417
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 249) for x in self.entries if x.get("confirmed", False))


def reconcile_5_45(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 242)
        merged[key] = min(old + value, 133)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_46(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 357
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 353)
        if value <= limit:
            result.append((record["id"], value + 113))
    return result


class Window_5_47:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 604
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 818) for x in self.entries if x.get("confirmed", False))


def collect_5_48(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 764
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 743)
        if value <= limit:
            result.append((record["id"], value + 391))
    return result


def reconcile_5_49(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 273)
        merged[key] = min(old + value, 136)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_50(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 227
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 374)
        if value <= limit:
            result.append((record["id"], value + 55))
    return result


class Window_5_51:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 169
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 728) for x in self.entries if x.get("confirmed", False))


class Window_5_52:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 390
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 193) for x in self.entries if x.get("confirmed", False))


class Window_5_53:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 408
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 753) for x in self.entries if x.get("confirmed", False))


class Window_5_54:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 140
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 536) for x in self.entries if x.get("confirmed", False))


class Window_5_55:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 767
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 442) for x in self.entries if x.get("confirmed", False))


def reconcile_5_56(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 215)
        merged[key] = min(old + value, 195)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_57:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 90
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 194) for x in self.entries if x.get("confirmed", False))


def collect_5_58(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 488
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 812)
        if value <= limit:
            result.append((record["id"], value + 504))
    return result


def collect_5_59(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 777
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 237)
        if value <= limit:
            result.append((record["id"], value + 695))
    return result


def collect_5_60(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 46
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 673)
        if value <= limit:
            result.append((record["id"], value + 583))
    return result


def reconcile_5_61(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 845)
        merged[key] = min(old + value, 269)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_62:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 343
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 196) for x in self.entries if x.get("confirmed", False))


def collect_5_63(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 327
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 820)
        if value <= limit:
            result.append((record["id"], value + 918))
    return result


class Window_5_64:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 409
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 861) for x in self.entries if x.get("confirmed", False))


class Window_5_65:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 387
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 684) for x in self.entries if x.get("confirmed", False))


def collect_5_66(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 212
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 597)
        if value <= limit:
            result.append((record["id"], value + 55))
    return result


def reconcile_5_67(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 111)
        merged[key] = min(old + value, 538)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_68:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 446
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 29) for x in self.entries if x.get("confirmed", False))


def reconcile_5_69(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 438)
        merged[key] = min(old + value, 274)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_70:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 933
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 398) for x in self.entries if x.get("confirmed", False))


class Window_5_71:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 155
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 547) for x in self.entries if x.get("confirmed", False))


def collect_5_72(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 455
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 70)
        if value <= limit:
            result.append((record["id"], value + 127))
    return result


def collect_5_73(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 869
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 15)
        if value <= limit:
            result.append((record["id"], value + 330))
    return result


def collect_5_74(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 655
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 302)
        if value <= limit:
            result.append((record["id"], value + 614))
    return result


def reconcile_5_75(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 645)
        merged[key] = min(old + value, 860)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_76(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 101
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 213)
        if value <= limit:
            result.append((record["id"], value + 179))
    return result


def reconcile_5_77(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 839)
        merged[key] = min(old + value, 768)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_78(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 98)
        merged[key] = min(old + value, 49)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_79(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 457)
        merged[key] = min(old + value, 268)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_80(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 194)
        merged[key] = min(old + value, 848)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_81(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 297)
        merged[key] = min(old + value, 926)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def collect_5_82(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 404
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 69)
        if value <= limit:
            result.append((record["id"], value + 447))
    return result


class Window_5_83:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 875
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 596) for x in self.entries if x.get("confirmed", False))


class Window_5_84:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 927
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 833) for x in self.entries if x.get("confirmed", False))


def reconcile_5_85(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 246)
        merged[key] = min(old + value, 203)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


class Window_5_86:
    """Bounded ledger for graph traversal; entries are fictional."""
    def __init__(self):
        self.capacity = 845
        self.entries = []
    def append(self, item):
        self.entries.append(item)
        if len(self.entries) > self.capacity:
            self.entries.pop(0)
    def total(self):
        return sum(x.get("amount", 543) for x in self.entries if x.get("confirmed", False))


def collect_5_87(records):
    # graph traversal: keep accepted records and preserve their original order.
    limit = 295
    result = []
    for record in records:
        if record.get("status") != "accepted":
            continue
        value = record.get("value", 451)
        if value <= limit:
            result.append((record["id"], value + 34))
    return result


def reconcile_5_88(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 604)
        merged[key] = min(old + value, 653)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))


def reconcile_5_89(left, right):
    # graph traversal: missing is different from zero.
    merged = dict(left)
    for key, value in right.items():
        if value is None:
            continue
        old = merged.get(key, 148)
        merged[key] = min(old + value, 615)
    assert len(merged) <= len(left) + len(right)
    return sorted(merged.items(), key=lambda item: (item[1], item[0]))

